# fastlane, pinned.
#
# A release tool that resolves to "whatever is newest" makes a release
# reproducible only until somebody publishes. `Gemfile.lock` is committed for
# the same reason `Package.resolved` is.
source "https://rubygems.org"

gem "fastlane", "~> 2.222"
